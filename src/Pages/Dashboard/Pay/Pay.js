import { Typography } from '@mui/material';
import { Box } from '@mui/system';
import React from 'react';

const Pay = () => {
    return (
        <Box>
            <Typography variant="h2" gutterBottom component="div" style={{margin: '11%'}}>
                <i>Payment is system coming soon</i>
            </Typography>
        </Box>
    );
};

export default Pay;